import React from "react";
import { StyleSheet, Text, View } from "react-native";

import CustomModal from "../../components/modal";

export default function FeedModal({ visible, setVisible }) {
  return (
    <CustomModal>
      <View></View>
    </CustomModal>
  );
}

const styles = StyleSheet.create({});
